name: fixture-base
version: 1.0.0
description: Base recipe that others depend on

generates:
  - path: base.txt
    content: "Base recipe applied"

  - path: package.json
    content: |
      {
        "name": "test",
        "version": "1.0.0"
      }

tests:
  - name: base.txt exists
    assert_file_exists: base.txt
